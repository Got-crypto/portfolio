export default function Projects({projectsSection}){
    return (
        <div tabIndex="0" ref={projectsSection} className="h-screen w-full drop-shadow-2xl bg-hero2 bg-cover bg-no-repeat bg-blend-screen bg-cyan-200 flex-col flex items-center justify-center">
            <div className="w-full max-w-screen-lg flex items-center h-full">
                I is projects
            </div>
        </div>
    )
}